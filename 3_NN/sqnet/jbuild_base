(jbuild_version 1)

(executables
  ((names (sqnet_base))
  (libraries (owl_base))
  (modes (byte native))
  ))

(rule
  ((targets (sqnet_base.js))
    (action
    (run ${bin:js_of_ocaml} --noruntime ${lib:js_of_ocaml-compiler:runtime.js}
      --source-map ${path:sqnet_base.bc} -o ${@} --pretty))))